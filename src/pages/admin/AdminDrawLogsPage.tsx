import { PlaceholderPage } from '../../shared/ui/PlaceholderPage';

export function AdminDrawLogsPage() {
  return (
    <PlaceholderPage
      title="Admin Draw Logs"
      description="Draw logs read-only placeholder."
      badgeLabel="Read-only"
      ctaLabel="로그 조회 준비 중"
    />
  );
}
